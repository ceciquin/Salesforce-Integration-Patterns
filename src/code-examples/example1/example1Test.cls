@isTest
private class example1Test {
    @isTest
    static void testExample1() {
        // Arrange: Set up any necessary data for the test
        // Example: Create test records or mock data

        // Act: Call the method you want to test
        // Example: example1.someMethod();

        // Assert: Verify the results
        // Example: System.assertEquals(expectedValue, actualValue);
    }
}